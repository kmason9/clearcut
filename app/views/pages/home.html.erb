<% @videos.each do |video|%>
  <%= video_tag video.clip.url,
      controls: false,
      autoplay: false,
      muted: true,
      loop: true,
      class: "video video-hover",
      data: { controller: "video",
              action: "mouseenter->video#play"
 } %>
<% end %>
